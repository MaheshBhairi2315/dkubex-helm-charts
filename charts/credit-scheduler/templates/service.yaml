apiVersion: v1
kind: Service
metadata:
  name: {{ include "credit-scheduler.fullname" . }}
  labels:
    {{- include "credit-scheduler.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "credit-scheduler.selectorLabels" . | nindent 4 }}
